/**
 * 请求配置：所有请求接口地址
 */

const mockHost = 'http://127.0.0.1:3001/'
const testHost = ''
const host = ''

module.exports = {
  testAjax: `${mockHost}test/testAjax`,
  testPost: `${mockHost}test/testPost`
}


